class Cart {
  constructor(productTitle, productPrice, imageUrl, quantity, sum) {
    this.productTitle = productTitle
    this.productPrice = productPrice
    this.imageUrl = imageUrl
    this.quantity = quantity
    this.sum = sum
  }
}

export default Cart
